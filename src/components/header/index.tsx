import React from 'react';
import { Box, Typography } from '@mui/material';

export const Header = () => (
  <Box p={2} sx={{ backgroundColor: 'secondary.dark' }}>
    <Typography variant="h3">Star Wars Adventure Team Setup</Typography>
  </Box>
)
